## 二维码流水从Redis迁移数据库 

### 一、目前的问题

1. 二维码流水存放Redis需要持久化，所以流水没有过期时间，一直占用Redis内存无法释放，导致Redis查询变慢
2. Redis主要是用来作缓存，并且Redis是单线程设计，容易发生链式阻塞反应
3. **重要** ：Redis不稳定，无法提供像数据库一样的事务支持，只能手动处理异常事件 undo 操作，当遇到并发时，没有处理并发锁时，就会导致一个请求发生异常，并被程序捕获，执行undo操作，Redis流水回滚，同时导致另一个请求生成错误重码数据
4. **并发锁的处理：** 
   - 目标：添加锁后不能影响其他型号的并发处理，要确定好锁对象的范围，同一时间，只允许一个请求操作一个流水记录，换到真实的业务上来说，就是一次要做很多批次的这个型号的PCB板，限制这个型号只能有一个批次能操作，其他批次阻塞等待（互斥锁）
   - 锁对象确定：二维码流水和规则绑定，规则和型号绑定，（可能不同的型号的规则相同，但是并发请求数据的情况机率极小），确定为型号id，partId

### 二、二维码流水数据库表设计

```sql
create table MES_QRCODE_SERIAL
(
    ID     VARCHAR2(32) not null primary key,
    CREATE_BY    VARCHAR2(32),
    CREATE_TIME  DATE,
    UPDATE_BY    VARCHAR2(32),
    UPDATE_TIME  DATE,
    RULE_PATH_PREFIX VARCHAR2(128),
    SERAIL_LENGTH INT,
    RULE_PATH_SUFFIX VARCHAR2(128),
    SERIAL INT,
    DEL_FLAG INT DEFAULT 0
)
/
comment on table MES_QRCODE_SERIAL is '二维码流水记录表'
/
comment on column MES_QRCODE_SERIAL.CREATE_BY is '创建人'
/
comment on column MES_QRCODE_SERIAL.CREATE_TIME is '创建日期'
/
comment on column MES_QRCODE_SERIAL.UPDATE_BY is '更新人'
/
comment on column MES_QRCODE_SERIAL.UPDATE_TIME is '更新日期'
/
comment on column MES_QRCODE_SERIAL.RULE_PATH_PREFIX is '规则路径前缀'
/
comment on column MES_QRCODE_SERIAL.SERAIL_LENGTH is '规则流水长度'
/
comment on column MES_QRCODE_SERIAL.RULE_PATH_SUFFIX is '规则路径后缀'
/
comment on column MES_QRCODE_SERIAL.SERIAL is '流水'
/
comment on column MES_QRCODE_SERIAL.DEL_FLAG is '逻辑删除标识（1：删除 0：未删除）'
```



### 三、修改地点和注意事项





### 四、拓展

####      重构框架

1. 用策略模式重构规则解析模块

   - QrcodeContext   解析上下文

   - QrcodeTranslateStrategy (interface):   void translate(QrcodeContext context);  策略接口
   - QrcodeTranslateManager  解析管理类       （1.策略解析 2.排除字符 3.进制 4.位数处理 5.其他操作等）返回 `List<MesQrcode>` 
   - QrcodeTranslateStrategyAnno (注解)   strategy =QrcodeTranslateStrategyEnum
   - QrcodeTranslateStrategyEnum (枚举)  String desc; --描述   Interger eleType;  类型

2. 用模板方法重构各供应商调用服务类

   - interface:   GenerateQrcode  ===> 返回格式化的二维码响应数据
   - abstract GenerateSetOrPcsByPnlTempalte  (SetPcsAcceptData setPcsAcceptData) implements  GenerateQrcode
   - 方法顺序：

     - 查询是否存在未下发的二维码数据，有，即返回 ====> 查询条件不一样
     - 是否重复打码
     - 卡控
   - 构建解析上下文
     - 查询方案、坐标
   - 调用 QrcodeTranslateManager 获取生成的二维码
     - 保存二维码
   - 二维码响应对象格式化

2. 代理模式功能加强，日志等统一操作GenerateQrcodeProxy

3. 工厂模式生成对应供应商的服务类（静态工厂模式）

      GenerateQrcodeFactory  ====> 返回  GenerateQrcodeProxy    根据不同的供应商生成对应的服务类，调用服务类直接返回格式化的二维码数据

